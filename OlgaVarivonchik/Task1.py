### Task 1.1 Write a Python program to calculate the length of a string without using the `len` function.

s = input("let's check how many length of a string: ")
total = 0
for v in s:
    total += 1
print(total)
